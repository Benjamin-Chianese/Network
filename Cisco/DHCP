Variables :

#IP_NETWORK# : IP network (ex : 192.168.1.0)
#IP_FIRST_POOL# : First IP of the pool (ex : 192.168.1.1)
#IP_LAST_POOL# : Last IP of the pool (ex : 192.168.1.254)
#IP_BEGIN_DHCP# : First DHCP IP address -1 (ex : 192.168.1.49)
#IP_END_DHCP# : Last DHCP IP address +1 (ex : 192.168.1.201)
#IP_GATEWAY# : LAN IP of the Gateway router (ex : 192.168.1.254)
#IP_MASK# : Network mask (ex : 255.255.255.0)
#IP_DNS# : IP dns selected  (ex : 8.8.8.8 8.8.8.4)


            ip dhcp excluded-address #IP_FIRST_POOL# #IP_BEGIN_DHCP#
            ip dhcp excluded-address #IP_END_DHCP# #IP_LAST_POOL#
            !
            ip dhcp pool 0 
            network #IP_NETWORK# #IP_MASK#
            dns-server #IP_DNS#
            default-router #IP_GATEWAY#
           
